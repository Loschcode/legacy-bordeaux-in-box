# this server is not available anymore
# therefore it's useless to try
# it's a legacy project.
production:
    scheme: sftp
    host: 178.62.239.135
    port: 22
    slots: 5
    user: root
    pass: lapetiteboxrocks
    check: true
    path:
        local: website
        remote: /var/www/website
    exclude: ["dploy.yaml"]
